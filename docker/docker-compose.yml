# docker/docker-compose.yml
version: '3.8'

services:
  opc-plc-server:
    image: mcr.microsoft.com/iotedge/opc-plc:latest
    container_name: opc-plc-production
    restart: unless-stopped
    ports:
      - "50000:50000"
    environment:
      - PLC_NAME=ProductionLine
    volumes:
      # init-nodes.json contains BOTH the production line nodes and the Boiler 3
      # nodes (as a FolderList). opc-plc's --nodesfile accepts only ONE file, so
      # a second --nodesfile would override the first instead of adding to it.
      - ./opc-plc/init-nodes.json:/app/init-nodes.json:ro
    command: >
      --pn=50000
      --nodesfile=/app/init-nodes.json
      --unsecuretransport
      --AutoAccept
      --StepNodeSampleRate=5000
    
    
